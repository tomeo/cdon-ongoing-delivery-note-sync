{
  "name": "cdon-ongoing-delivery-note-sync",
  "version": "0.2.0",
  "description": "Fetches delivery notes from CDON and posts them to the corresponding orders in Ongoing",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write \"**/*.+(js|md|yml|yaml)\"",
    "start": "node index.js"
  },
  "author": "Tommy Ivarsson",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.0"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "prettier-eslint-cli": "^5.0.0",
    "serverless-iam-roles-per-function": "^2.0.2"
  }
}
